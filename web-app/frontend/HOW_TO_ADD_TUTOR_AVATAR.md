# Как добавить аватарку тьютора

## Проблема
Файл `tutor-avatar.jpg` был поврежден при коммите (Git обработал его как текст вместо binary).

## Решение

### Временное (сейчас используется):
- В демо-хуке: эмодзи робота 🤖 в градиенте
- В ConversationScreen: `<BotIcon>` компонент

### Как добавить правильное изображение:

1. **Подготовьте изображение:**
   - Формат: JPG, PNG, или WebP
   - Размер: 96x96 пикселей (минимум)
   - Вес: < 100KB (оптимизируйте через TinyPNG или Squoosh)

2. **Добавьте файл:**
   ```bash
   # Скопируйте файл в public
   cp your-avatar.jpg web-app/frontend/public/tutor-avatar.jpg
   
   # Убедитесь, что Git обрабатывает как binary
   git add -f web-app/frontend/public/tutor-avatar.jpg
   
   # Проверьте, что файл валидный
   file web-app/frontend/public/tutor-avatar.jpg
   # Должно быть: "JPEG image data" или "PNG image data"
   ```

3. **Раскомментируйте использование:**

   В `Funnel.tsx` (строка ~265):
   ```tsx
   // Заменить
   <div className="w-24 h-24 rounded-full bg-gradient-to-br from-cyan-500 to-cyan-700 flex items-center justify-center border-2 border-cyan-500/50 shadow-lg text-4xl">
       🤖
   </div>
   
   // На
   <img 
       src="/tutor-avatar.jpg" 
       alt="AI Tutor" 
       className="w-24 h-24 rounded-full object-cover border-2 border-cyan-500/50 shadow-lg" 
   />
   ```

   В `FunnelApp.tsx` (строка ~97):
   ```tsx
   tutorAvatarUrl="/tutor-avatar.jpg"
   ```

4. **Протестируйте локально:**
   ```bash
   npm run dev
   # Откройте http://localhost:3000/welcome
   ```

5. **Задеплойте:**
   ```bash
   git commit -m "feat: add tutor avatar image"
   git push origin main
   ```

## Альтернатива: использовать внешний URL

Если не хотите коммитить изображение в репозиторий:

```tsx
tutorAvatarUrl="https://your-cdn.com/tutor-avatar.jpg"
```

Преимущества:
- Не увеличивает размер репозитория
- Можно менять изображение без деплоя
- Работает сразу на всех окружениях

Недостатки:
- Зависимость от внешнего сервиса
- Медленнее загружается (отдельный HTTP-запрос)

